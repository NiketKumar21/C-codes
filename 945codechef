#include <stdio.h>

int main() {
int t;
scanf("%d",&t);
int i;
for(i=1;i<=t;i=i+1)
{
    int N;
    scanf("%d",&N);
    if(N<2)
    {
        printf("%d\n",200);
    }
    else
    {
        printf("%d\n",100*N);
    }
}

}

