#include <stdio.h>
#include <math.h>

int main() {

int t;
scanf("%d",&t);
int i;
for(i=1;i<=t;i=i+1)
{ int a,b;
scanf("%d %d",&a,&b);

int k,d;
for(k=a;k<=b;k=k+1)
{ d=0;
    int f;
    for(f=2;f<=sqrt(k);f=f+1)
    {
        if(k%f==0)
        {
            d=1;
            
            break;
        }
        
    }
    if(d==0 && k!=1)
    {
        printf("%d\n",k);
    }
}

    
}
}
